A static site generator to convert files from markdown to HTML.

# Usage:

Create markdown files inside `contents` folder and run `python main.py`. This will recursively convert Markdown files to HTML files. The `Static` folder holds all the images referenced in your markdown file.
